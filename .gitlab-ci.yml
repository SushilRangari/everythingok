
build-job:
  stage: build
  script:
    - echo "Hieee, $GITLAB_USER_LOGIN!"
  rules:
    - if: '$CI_COMMIT_TAG =~ /^\d+\.\d+\.DEV\d+/'
   
  tags:
    - dish2
  
test-job1:
  stage: test
  script:
    - echo "installed git, made entry of svrgitlab on gitlab server to resolve locally"
  tags:
    - dish2
  only:
    variables:
      - $CI_COMMIT_MESSAGE =~/((DEVRELEASE))/
Test-job2:
  stage: test
  script:
    - echo "This job tests something, but takes more time than test-job1."
  rules:
    - if: '$CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+\.DEVRELEASE\.\d+$/'
  tags:
    - dish2
deploy-prod:
  stage: deploy
  script:
    - echo "This job deploys something from the $CI_COMMIT_TAG branch."
  tags:
    - dish2
  only:
    - /v1\/release\
